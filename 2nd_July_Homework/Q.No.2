Problem 2 – Two Sum
LeetCode: 1

Topic
HashMap

Concepts Practiced
HashMap
put()
get()
containsKey()


Solution of the problem:-

-->
class Solution {
public:
    vector<int> twoSum(vector<int> &nums, int target) {
      
    for (int i = 0; i < nums.size(); i++) {
       for (int j = i + 1; j < nums.size(); j++) {
           if (nums[j] == target - nums[i]) {
              return {i, j};
                }
              } 
           }
    // Return an empty vector if no solution is founding in the array.
        return {};
    }
};
